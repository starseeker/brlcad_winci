// Auto-generated by ged_cmd_scanner for command exec API wrappers
#include "ged/ged_cmds.h"
int ged_exec_3ptarb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "3ptarb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_B(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "B";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_C2(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "C2";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_C3(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "C3";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_CDT(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "CDT";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_CDT2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "CDT2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_CDTm2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "CDTm2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_CDTn(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "CDTn";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_CDTn2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "CDTn2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_CDTnw(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "CDTnw";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_CDTp2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "CDTp2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_CDTw(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "CDTw";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_E(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "E";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_EC(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "EC";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_EOF(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "EOF";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_F(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "F";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_F2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "F2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_FSBB(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "FSBB";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_FSBB2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "FSBB2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_FTBB(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "FTBB";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_FTBB2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "FTBB2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_FTD(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "FTD";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_I(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "I";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_L(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "L";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_L2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "L2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_N(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "N";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_S(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "S";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_SB(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "SB";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_SCV(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "SCV";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_SK(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "SK";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_SK2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "SK2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_SN(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "SN";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_SUV(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "SUV";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_SUVP(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "SUVP";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_T(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "T";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_T2d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "T2d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_TB(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "TB";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_V(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "V";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_Volume(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "Volume";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_Z(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "Z";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_a(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "a";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_accept(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "accept";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_adc(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "adc";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_add(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "add";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_adj_air(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "adj_air";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_adjust(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "adjust";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ae(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ae";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ae2dir(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ae2dir";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_aet(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "aet";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_analyze(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "analyze";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_anchor(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "anchor";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_anim(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "anim";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_annotate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "annotate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_append(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "append";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_apropos(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "apropos";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_arb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "arb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_arced(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "arced";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_area(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "area";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_arot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "arot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_arrow(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "arrow";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_art(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "art";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_attach(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "attach";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_attr(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "attr";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_autoview(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "autoview";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_axes(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "axes";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_axes_color(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "axes_color";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ballpivot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ballpivot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_beq(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "beq";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bev(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bev";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bg(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bg";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bge(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bge";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bgt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bgt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_blast(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "blast";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ble(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ble";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_blt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "blt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bne(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bne";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bo(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bo";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bool(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bool";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_condense(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_condense";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_decimate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_decimate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_dump(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_dump";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_exterior(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_exterior";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_face_fuse(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_face_fuse";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_face_sort(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_face_sort";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_flip(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_flip";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_fuse(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_fuse";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_merge(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_merge";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_smooth(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_smooth";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_split(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_split";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_sync(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_sync";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bot_vertex_fuse(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bot_vertex_fuse";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_bots(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "bots";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_brep(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "brep";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c2_create_line(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c2_create_line";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c2_remove(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c2_remove";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_copy(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_copy";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_create(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_create";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_flip(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_flip";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_in(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_in";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_insert_knot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_insert_knot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_interp(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_interp";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_join(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_join";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_move(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_move";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_remove(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_remove";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_set_cv(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_set_cv";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_split(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_split";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_c3_trim(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "c3_trim";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_cat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "cat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_cc(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "cc";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_cdim(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "cdim";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_center(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "center";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_center_dot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "center_dot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_check(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "check";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_chull(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "chull";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_clean(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "clean";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_clear(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "clear";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_clone(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "clone";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_close(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "close";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_closedb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "closedb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_coil(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "coil";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_collapse(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "collapse";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_color(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "color";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_comb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "comb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_comb_color(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "comb_color";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_comb_std(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "comb_std";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_combmem(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "combmem";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_concat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "concat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_constraint(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "constraint";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_copy(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "copy";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_copyeval(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "copyeval";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_copymat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "copymat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_cp(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "cp";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_cpi(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "cpi";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_create(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "create";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_csg(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "csg";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_d(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "d";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_data_lines(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "data_lines";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_db_glob(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "db_glob";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dbconcat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dbconcat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dbfind(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dbfind";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dbip(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dbip";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dbot_dump(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dbot_dump";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dbversion(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dbversion";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_debug(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "debug";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_debugbu(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "debugbu";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_debugdir(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "debugdir";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_debuglib(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "debuglib";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_debugnmg(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "debugnmg";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_decimate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "decimate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_decompose(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "decompose";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_degen_faces(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "degen_faces";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_del(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "del";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_delay(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "delay";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_delete(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "delete";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dim(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dim";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dir2ae(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dir2ae";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dm(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dm";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dplot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dplot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_draw(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "draw";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dsp(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dsp";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dump(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dump";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_dup(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "dup";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_e(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "e";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_e_create(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "e_create";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_eac(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "eac";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_echo(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "echo";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_edarb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "edarb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_edcodes(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "edcodes";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_edcolor(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "edcolor";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_edcomb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "edcomb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_edgedir(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "edgedir";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_edit(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "edit";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_editit(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "editit";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_edmater(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "edmater";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_end(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "end";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_env(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "env";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_erase(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "erase";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ert(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ert";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ev(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ev";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_eval(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "eval";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_exists(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "exists";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_exit(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "exit";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_expand(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "expand";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_export(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "export";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_extra_edges(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "extra_edges";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_extrude(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "extrude";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_eye(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "eye";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_eye_pos(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "eye_pos";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_eye_pt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "eye_pt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_f_create(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "f_create";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_f_rev(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "f_rev";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_facedef(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "facedef";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_faceplate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "faceplate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_facetize(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "facetize";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_facetize_old(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "facetize_old";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_fb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "fb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_fb2pix(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "fb2pix";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_fbclear(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "fbclear";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_fill(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "fill";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_fill_color(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "fill_color";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_find(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "find";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_find_arb_edge(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "find_arb_edge";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_find_bot_edge(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "find_bot_edge";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_find_bot_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "find_bot_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_find_pipe_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "find_pipe_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_flip(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "flip";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_flipped_edges(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "flipped_edges";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_form(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "form";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_fracture(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "fracture";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_g(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "g";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_garbage_collect(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "garbage_collect";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_gdiff(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "gdiff";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_gen(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "gen";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_geo(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "geo";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_get(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "get";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_get_autoview(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "get_autoview";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_get_bot_edges(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "get_bot_edges";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_get_comb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "get_comb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_get_dbip(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "get_dbip";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_get_eyemodel(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "get_eyemodel";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_get_type(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "get_type";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_glob(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "glob";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_gobjs(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "gobjs";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_gqa(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "gqa";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_graph(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "graph";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_grid(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "grid";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_grid2model_lu(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "grid2model_lu";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_grid2view_lu(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "grid2view_lu";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_group(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "group";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_h(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "h";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_heal(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "heal";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_height(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "height";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_help(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "help";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_hide(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "hide";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_holds(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "holds";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_how(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "how";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_human(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "human";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_i(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "i";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_idents(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "idents";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_illum(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "illum";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_import(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "import";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_importFg4Section(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "importFg4Section";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_in(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "in";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_independent(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "independent";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_info(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "info";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_initmsg(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "initmsg";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_insert(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "insert";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_inside(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "inside";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_instance(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "instance";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_intersect(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "intersect";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_irect(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "irect";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_isect(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "isect";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_isize(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "isize";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_item(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "item";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_joint(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "joint";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_joint2(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "joint2";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_keep(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "keep";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_keypoint(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "keypoint";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_kill(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "kill";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_killall(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "killall";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_killrefs(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "killrefs";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_killtree(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "killtree";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_knob(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "knob";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_l(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "l";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_l_create(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "l_create";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_label(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "label";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_label_color(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "label_color";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_labelface(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "labelface";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_labelvert(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "labelvert";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_lc(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "lc";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_lcnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "lcnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_length(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "length";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_line(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "line";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_line_style(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "line_style";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_line_width(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "line_width";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_lint(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "lint";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_list(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "list";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_listeval(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "listeval";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_load(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "load";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_loadview(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "loadview";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_lod(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "lod";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_log(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "log";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_lookat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "lookat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_lookat_pt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "lookat_pt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ls(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ls";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_lt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "lt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_m2v_point(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "m2v_point";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_make(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "make";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_make_name(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "make_name";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_make_pnts(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "make_pnts";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_man(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "man";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_manifold(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "manifold";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mat4x3pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mat4x3pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mat_ae(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mat_ae";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mat_mul(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mat_mul";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mat_scale_about_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mat_scale_about_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_match(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "match";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mater(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mater";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_material(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "material";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mesh(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mesh";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_metaball_delete_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "metaball_delete_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_metaball_move_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "metaball_move_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mirface(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mirface";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mirror(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mirror";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_model2grid_lu(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "model2grid_lu";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_model2view(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "model2view";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_model2view_lu(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "model2view_lu";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_model_axes(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "model_axes";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mouse_add_metaball_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mouse_add_metaball_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mouse_append_pipe_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mouse_append_pipe_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mouse_move_metaball_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mouse_move_metaball_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mouse_move_pipe_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mouse_move_pipe_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mouse_prepend_pipe_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mouse_prepend_pipe_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_move(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "move";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_move_all(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "move_all";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_move_arb_edge(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "move_arb_edge";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_move_arb_face(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "move_arb_face";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mrot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mrot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_multiview(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "multiview";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mv(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mv";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_mvall(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "mvall";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_n(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "n";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nirt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nirt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_cmface(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_cmface";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_collapse(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_collapse";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_fix_normals(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_fix_normals";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_kill_f(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_kill_f";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_kill_v(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_kill_v";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_make_v(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_make_v";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_mm(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_mm";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_move_v(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_move_v";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_nmg_simplify(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "nmg_simplify";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_npush(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "npush";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_o(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "o";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_obj(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "obj";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_objs(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "objs";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ocenter(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ocenter";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_open(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "open";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_open_edges(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "open_edges";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_opendb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "opendb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_opt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "opt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_orient(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "orient";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_orientation(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "orientation";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_orotate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "orotate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_oscale(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "oscale";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_otranslate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "otranslate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_overlap(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "overlap";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_overlay(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "overlay";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_p(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "p";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_params(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "params";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pathlist(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pathlist";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_paths(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "paths";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pathsum(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pathsum";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pca(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pca";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_permute(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "permute";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_perspective(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "perspective";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pick(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pick";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pipe_append_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pipe_append_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pipe_delete_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pipe_delete_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pipe_move_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pipe_move_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pipe_prepend_pnt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pipe_prepend_pnt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pix2fb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pix2fb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_plate_mode(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "plate_mode";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_plot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "plot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pmat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pmat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pmodel2view(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pmodel2view";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_png(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "png";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_png2fb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "png2fb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pngwf(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pngwf";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pnts(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pnts";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_polygon(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "polygon";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pos(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pos";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pos_only(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pos_only";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_postscript(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "postscript";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_prcolor(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "prcolor";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_prefix(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "prefix";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_preview(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "preview";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_print(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "print";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_process(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "process";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_protate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "protate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ps(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ps";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pscale(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pscale";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pset(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pset";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ptranslate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ptranslate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_pull(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "pull";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_push(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "push";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_put(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "put";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_put_comb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "put_comb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_putmat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "putmat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_q(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "q";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_qray(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "qray";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_quat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "quat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_query_ray(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "query_ray";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_quit(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "quit";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_qvrot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "qvrot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_r(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "r";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rcodes(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rcodes";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_read(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "read";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rect(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rect";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_red(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "red";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_redraw(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "redraw";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_regdef(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "regdef";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_region(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "region";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_regions(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "regions";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_reject(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "reject";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_remesh(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "remesh";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_remove(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "remove";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_reopen(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "reopen";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_repair(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "repair";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_res_h(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "res_h";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_res_major_h(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "res_major_h";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_res_major_v(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "res_major_v";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_res_v(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "res_v";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rfarb(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rfarb";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rm(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rm";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rmap(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rmap";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rmat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rmat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rmater(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rmater";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rot_about(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rot_about";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rot_point(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rot_point";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rotate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rotate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rotate_arb_face(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rotate_arb_face";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rrt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rrt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rselect(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rselect";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rtabort(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rtabort";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rtarea(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rtarea";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rtcheck(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rtcheck";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rtedge(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rtedge";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rtweight(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rtweight";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_rtwizard(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "rtwizard";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_birail(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_birail";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_copy(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_copy";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_create(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_create";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_ext_c3(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_ext_c3";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_ext_v(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_ext_v";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_interp(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_interp";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_move(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_move";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_remove(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_remove";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_revolution(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_revolution";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_set_cv(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_set_cv";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_split(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_split";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_tensor(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_tensor";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_s_trim(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "s_trim";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_save(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "save";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_savekey(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "savekey";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_saveview(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "saveview";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_sca(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "sca";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_scale(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "scale";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_screen_grab(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "screen_grab";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_screengrab(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "screengrab";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_sdata_lines(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "sdata_lines";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_search(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "search";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_select(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "select";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_selection(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "selection";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_selections(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "selections";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_send(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "send";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_set(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "set";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_set_output_script(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "set_output_script";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_set_transparency(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "set_transparency";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_set_uplotOutputMode(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "set_uplotOutputMode";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_setview(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "setview";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_shaded_mode(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "shaded_mode";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_shader(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "shader";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_shells(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "shells";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_show(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "show";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_showmats(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "showmats";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_shrink_surfaces(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "shrink_surfaces";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_simulate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "simulate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_size(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "size";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_slew(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "slew";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_smooth(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "smooth";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_snap(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "snap";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_solid(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "solid";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_solid_report(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "solid_report";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_solids(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "solids";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_solids_on_ray(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "solids_on_ray";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_solve(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "solve";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_sphgroup(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "sphgroup";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_split(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "split";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_spsr(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "spsr";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_start(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "start";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_stat(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "stat";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_step(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "step";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_strip(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "strip";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_subd(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "subd";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_subtract(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "subtract";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_summarize(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "summarize";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_summary(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "summary";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_sv(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "sv";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_sync(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "sync";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_t(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "t";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_t_create(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "t_create";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tables(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tables";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tick(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tick";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tick_color(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tick_color";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tick_interval(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tick_interval";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tick_length(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tick_length";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tick_major_color(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tick_major_color";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tick_major_length(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tick_major_length";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tick_threshold(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tick_threshold";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ticks_per_major(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ticks_per_major";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tikz(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tikz";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tire(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tire";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_title(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "title";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tol(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tol";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_topo(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "topo";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tops(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tops";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tra(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tra";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_track(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "track";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_translate(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "translate";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tree(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tree";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_tri(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "tri";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_triple_color(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "triple_color";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_type(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "type";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_types(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "types";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_unhide(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "unhide";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_unit(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "unit";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_units(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "units";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_unload(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "unload";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_update(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "update";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_v(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "v";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_v2m_point(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "v2m_point";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_vZ(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "vZ";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_v_create(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "v_create";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_v_remove(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "v_remove";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_valid(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "valid";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_vdraw(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "vdraw";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_version(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "version";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_view(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "view";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_view2(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "view2";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_view2grid_lu(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "view2grid_lu";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_view2model(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "view2model";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_view2model_lu(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "view2model_lu";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_view2model_vec(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "view2model_vec";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_view_axes(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "view_axes";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_view_func(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "view_func";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_viewdir(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "viewdir";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_viewrot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "viewrot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_viewsize(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "viewsize";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_vlist(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "vlist";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_vnirt(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "vnirt";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_voxelize(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "voxelize";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_vquery_ray(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "vquery_ray";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_vrot(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "vrot";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_vslew(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "vslew";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_wcodes(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "wcodes";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_weld(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "weld";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_whatid(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "whatid";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_which(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "which";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_which_shader(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "which_shader";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_whichair(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "whichair";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_whichid(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "whichid";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_who(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "who";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_width(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "width";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_wmater(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "wmater";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_write(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "write";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_x(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "x";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_xpush(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "xpush";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_y(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "y";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_ypr(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "ypr";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_zap(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "zap";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
int ged_exec_zoom(struct ged *gedp, int argc, const char **argv) {
    static const char *cmdname = "zoom";
    if (!argv) return BRLCAD_ERROR;
    if (argc > 0 && !argv[0]) argv[0] = cmdname;
    if (!argv[0]) argv[0] = cmdname;
    return ged_exec(gedp, argc, argv);
}
